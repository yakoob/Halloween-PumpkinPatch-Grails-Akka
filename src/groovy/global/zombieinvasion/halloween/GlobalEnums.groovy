package global.zombieinvasion.halloween

enum Type {
    POWER_SWITCH, SENSOR, SERVO
}

enum Status {
    ON, OFF
}

enum Device {
    MOTION,
    SMOKE,
    LIGHTS,
    MUSIC,
    PROJECTOR,
    AUDIO,
    VIBRATION
}
